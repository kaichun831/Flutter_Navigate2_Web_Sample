import 'package:flutter/material.dart';
import 'package:kaichun_profile/model/Work.dart';

class WorksDetialScreen extends StatelessWidget {
  Work? work;
  WorksDetialScreen({Key? key,required this.work});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('1'),
      ),
    );
  }
}
